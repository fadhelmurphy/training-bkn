import React, { useState } from 'react';

export default function Siblings({count}) {
  // Declare a new state variable, which we'll call "count"
//   const [count, setCount] = useState(0);

  return (
    <>
    <h1 className="colorH1">Keranjang {count}</h1>
    </>
  );
}